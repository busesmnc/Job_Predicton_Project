linkedin_scraper/
│── scrape_jobs.py   # LinkedIn'den iş ilanlarını çeken kod
│── save_to_db.py    # Çekilen veriyi SQLite'a kaydeden kod
│── linkedin_jobs.db  # SQLite veri tabanı (oluşacak)
│── requirements.txt  # Gerekli kütüphaneler (isteğe bağlı)
